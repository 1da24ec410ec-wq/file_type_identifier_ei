# file_type_identifier_ei
Identify any file's true type without relying on its name. File Type Identifier inspects binary magic bytes first — catching renamed or extension-less files — then falls back to extension and MIME type detection. Covers 30+ binary formats (images, audio, video, executables, archives) and 60+ code/text extensions out of the box, all with pure Python stdlib.
